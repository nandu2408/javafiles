// in js datatypes are divided into two types primitive and non primitive data types

// in primitive datatypes they are string, boolean, int , undefined
// strings means sequence or group character eclosed withinthe single, duble or blackticks is called strings

// strings are enclosed with in the " " doubl quotes
let name ="nanda kumar";
console.log(name);

// strings are enclosed within the ' ' single quotes
let name2= 'chaitainya';
console.log(name2);

// strings are also encloed with in the baCKTICKS ALSO
let name3 = `rachana`;
console.log(name3);

// js numbers are used to define the numeric values in the javascript

let num =3;
console.log(num);

let num2= -3;
console.log(num2);
let num3=9.5;
console.log(num3);

// boolean in js

let unmatched = true;
let matched =false;
console.log(unmatched);
console.log(matched);

// in js object is a non primitive datatype

let obj1 ={
    name : "kumar",
    age : 34,
    caste : "bc -d",
    address : "moolpalli"
}
console.log(obj1.age);
console.log(obj1);

// we are tpeOf() function for checking the which datatype of a variable

// like this

console.log(typeof(obj1));
console.log(typeof(num))

// js is dynamically typed language noneed to specify the datatype 