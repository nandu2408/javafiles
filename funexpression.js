 let add= function(n1, n2) 
//  here we are not defining the fuction
// we are assigning the function to the variable
// so, this functio is called anonymous function
// in this anonymous we doesn't have function name for calling
// in this case we calling the function with the assigning variable.
// anything we are assigning to the variable and we can evalute them is called expression
{
    return n1+n2;
}
let result=add(4,4);
console.log(result);